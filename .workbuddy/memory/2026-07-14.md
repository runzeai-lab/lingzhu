# 2026-07-14 工作记忆

## GitHub 自动备份（03:05 自动化）
- **状态**: ✅ 推送成功（双 commit）
- **Commits**: `32b27ce`（清理 2026-07-04 遗留 staged 记录） + `da7e92c`（写入今日日志）
- **环境变化（重要）**: 原 WSL 备份脚本 `E:\WorkBuddy\Claw\scripts\auto_backup_github.sh` **已不存在**；且本沙箱中 `wsl` 无法访问 `/mnt/e/WorkBuddy/Claw`（MSYS 把路径错误转换、E: 在 WSL 不可见），故 WSL 备份路径已不可行。
- **新方案**: 直接用 Git Bash 完成 commit + `git push origin main`（SSH，id_ed25519，exit=0）。仅 2 个 metadata 文件改动，`git status` 未触发 Windows Git 2.54 mmap bug，无需 WSL。
- **踩坑**: 提交时遇到 2 个陈旧锁文件 `.git/HEAD.lock`、`refs/heads/main.lock`（源自 7-05 后台任务超时），`find .git -name "*.lock" -delete` 清理后提交成功。
- **结论**: 后续 GitHub 备份自动化应改为 Git Bash 直连方案，不再依赖 WSL 脚本。

## GitHub 自动备份（03:08 自动化 · 第二次）
- **指令**: 运行 WSL 脚本 `wsl bash /mnt/e/WorkBuddy/Claw/scripts/auto_backup_github.sh`
- **WSL 结果**: ❌ 两次均失败（exit 127）。`wsl` 命令在本 Git Bash 沙箱中不被识别，路径被 MSYS 错误转换为 `C:/Program Files/Git/mnt/e/...`；E: 在 WSL 不可见。
- **降级方案**: 沿用 03:05 已验证的 Git Bash 直连 SSH 推送（脚本逻辑等价执行：add → commit → push）。
- **待提交**: 仅 `.workbuddy/memory/2026-07-14.md`（含 03:05 记录，此前未提交）。
- **结论**: WSL 备份路径在本沙箱彻底不可行，自动化应稳定使用 Git Bash SSH 方案。
