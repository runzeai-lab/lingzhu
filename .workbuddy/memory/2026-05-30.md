# 2026-05-30 工作记忆

## GitHub 自动备份
- **时间**: 23:00
- **状态**: ✅ 推送成功
- **Commit**: f8b9d04 (`自动备份: V190.0 2026-05-30_23:00`)
- **文件数**: 929 files, +135460/-133800
- **方式**: PowerShell + WSL HTTPS
- **关键发现**: 
  - Git Bash HTTPS 443 被阻断（`Connection was reset`），但 WSL 网络栈可达 GitHub
  - Git Bash 的 `wsl` 命令被 MSYS 路径翻译破坏（`/mnt/e` → `C:/Program Files/Git/mnt/e`），必须通过 PowerShell 调用 WSL
  - WSL 中 `api.github.com` 可达但 PAT Token 仅 `repo` 权限，无法注册 SSH 密钥（需 `admin:public_key` scope）
  - SSH 密钥存在（`~/.ssh/id_ed25519`）但未在 GitHub 账户注册
