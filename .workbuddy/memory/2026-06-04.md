# 2026-06-04 工作记忆

## GitHub 自动备份 (23:00)
- **状态**: ✅ 推送成功
- **Commit**: 8124074 (`自动备份: V191.2 2026-06-04_23:00:51`)
- **方式**: WSL git (commit) + Git Bash SSH (push)
- **踩坑与修复**:
  - Windows Git 2.54.0 存在 mmap bug，`git status`/`git add` 均报 `fatal: mmap failed: Invalid argument`
  - 根因：HEAD tree 中包含 641 个绝对路径文件 (`E:/WorkBuddy/Claw/debug_screenshots/...`、`xiaohongshu_user_data*/...`)，源于历史提交中 Chrome user data 被错误 `git add` 提交
  - 修复：WSL (Git 2.43.0) 不受 mmap bug 影响，通过 `git ls-tree -r HEAD --name-only | grep '^E:/' | while read f; do git rm --cached "$f"; done` 清理所有绝对路径文件
  - 随后正常 `git add .` + commit，Git Bash SSH push 成功
  - WSL 中 SSH 密钥未配置（`Permission denied (publickey)`），故 push 必须用 Git Bash
